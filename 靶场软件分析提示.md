# huan.apk 静态验证记录

## 样本
- APK: `huan.apk`
- 云端样本: `payload.so`
- 包名（AndroidManifest）: `com.huanty.jkchess`
- APK 中的 arm64 原生库: `libhydcc.so`, `libhyprotect.so`, `libhyvmp.so`, `libinjector.so`, `libsample_so.so`

## 哈希（基线）
- huan.apk SHA-256: `92FEDF59491C607E17337D2F6C19F02FA304B02160859B82969F33E1E572D124`
- payload.so SHA-256: `F9AC3A88366D04446F6EE0686A722FD22B36D5783E3B3B07F950606B02B60024`

## 代码路径证据
反编译的 `com.kkstudio.rootso.ApiClient` 暴露以下 native 接口：
- `login(String, Context)`
- `fetchSoDownloadUrl()`
- `downloadFile(String, String[, OnProgressListener])`
- `fetchVariable(String)`
- `getSavedCard(Context)`

`MainActivity` 暴露以下流程方法：
- `fetchCloudSoWithVersionCheck()`
- `doInject(boolean)`
- `launchApp(String)`
- `copyToLocalCache(Uri, String)`

`Injector` 暴露：
- `deployInjector()`
- `deployPayload(String, String, int, int)`
- `inject(int, String[, String, InjectOptions])`

## 原生字符串观察
`libhydcc.so` 中出现授权/下载相关类名与字符串（包括 `LoginResult`、`card_info.txt`、`fetchSoDownloadUrl`、`downloadFile`），以及两个 HTTPS API 主机名：
- `ops.new-cdn.com`
- `yz.blyfw.cn`

`libinjector.so` 中出现 AndKittyInjector、远程 `dlopen`/`JNI_OnLoad`、memfd、寄存器/断点等注入实现字符串。

`payload.so` 为 ELF64 AArch64（machine 183），包含 Dear ImGui overlay 相关字符串，并引用：
- `/data/user/0/com.tencent.jkchess/files/live2d`
- `/sdcard/huanty_recent.log`
- `/sdcard/huanty/image`

## 结论
1. 授权登录、云端 SO 地址获取和文件下载位于 native 层，Java 反编译结果无法独立还原完整签发/校验算法。
2. 应用确实包含“部署 injector → 选择/下载 payload → 对目标进程执行注入”的完整链路。
3. 目录中的 `payload.so` 与 APK 内置库均为 AArch64 ELF；直接把外部样本替换为内置文件会涉及重打包、签名和运行时完整性校验。
4. 本记录只做静态验证，没有修改 APK、没有改写 `payload.so`、没有执行进程注入，也没有生成或伪造授权密钥。

## 可复核命令
```powershell
Get-FileHash .\huan.apk -Algorithm SHA256
Get-FileHash .\payload.so -Algorithm SHA256
```
